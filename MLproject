name: sample demo

conda_env: conda.yaml

entry_points:
    main:
        parameters:
            first: {type: int, default: 2}
            second: {type: int, default: 5}
        command: "python demo.py -f {first} -s {second}"